import {
    atom,
  } from 'recoil'
  
  export const ambiencesAtom = atom({
    key: 'ambiencesAtom',
    default: {},
  })

  export const peopleAtom = atom({
    key: 'peopleAtom',
    default: {},
  })

  export const placesAtom = atom({
    key: 'placesAtom',
    default: {},
  })

  export const wikiAtom = atom({
    key: 'wikiAtom',
    default: {},
  })
