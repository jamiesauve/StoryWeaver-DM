// const sqlite3 = require('sqlite3').verbose()

// // this is not a relative path from this file - I think it needs to be relative to the package.json, but I'm not sure.
// const db = new sqlite3.Database('./pseudoServer/database/story_weaver.db', sqlite3.OPEN_READWRITE, err => {
//   if (err) console.log('error during db connection:', err)
//   else console.log('successfully connected to the db.')
// })

// // module.exports = db;